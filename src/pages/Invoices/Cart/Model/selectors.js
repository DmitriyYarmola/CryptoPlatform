export const ordersTableData = state => state.CartReducer.invoiceItems

export const count = state => state.CartReducer.count

export const current = state => state.CartReducer.current

export const isCartOpen = state => state.CartReducer.isCartOpen

export const isLoading = state => state.CartReducer.isLoading

export const invoiceById = state => state.CartReducer.invoiceById

export const rate = state => state.CartReducer.rate

export const rateUSD = state => state.CartReducer.rateUSD
