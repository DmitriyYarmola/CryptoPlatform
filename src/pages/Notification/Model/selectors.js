export const isLoading = state => state.NotificationReducer.isLoadi

export const notifications = state => state.NotificationReducer.notifications

export const notFound = state => state.NotificationReducer.notFound
