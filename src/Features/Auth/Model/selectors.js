export const isLoading = state => state.AuthReducer.isLoading

export const invalidCredentials = state => state.AuthReducer.invalidCredentials

export const isAuthenticated = state => state.AuthReducer.isAuthenticated

export const token = state => state.AuthReducer.token

export const merchants = state => state.AuthReducer.merchants
